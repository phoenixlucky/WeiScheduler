{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the WeiScheduler desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "autostart:default"]
}

# Changelog

## 1.6.1 - 2026-06-29

### 🎨 可读性优化

- **全部 6 套皮肤对比度全面修复**
  - 5 套暗色主题背景从纯黑/近黑提升至深色可读范围，减轻眼睛负担
  - 面板不透明度从 0.60~0.68 提升至 0.76~0.82，减少背景渐变干扰
  - 主文字色（`--text`）全面加亮，辅助文字色（`--muted`）提高对比度
  - 所有皮肤新增 `--text-secondary` / `--text-tertiary` 文字层级变量
  - 浅色主题（豆蔻少女）主文字加深以优化可读性
  - `styles.css` 中通过 `var(--text-secondary, var(--muted))` 保障旧皮肤兼容

### 🔧 技术变更

- `package.json`: version 1.6.0 → 1.6.1
- 全部 6 个皮肤 CSS 文件色值调整

## 1.6.0 - 2026-06-28

### 🐛 修复

- **修复定时任务有时会消失的严重 Bug**
  - 根因 1：`tasks.json` 解析失败（JSON 损坏/半写入）时会直接清空文件，所有任务永久丢失 — 现已改为仅返回空内存对象，保留原文件供恢复
  - 根因 2：异步回调（进程 close/error 事件）间写入无序列化，竞争条件导致数据覆盖丢失 — 新增写互斥队列（Promise 链）保证写入顺序
  - 所有写入操作改为原子写入（临时文件 → rename，防止系统崩溃导致半写入）
  - 所有 Express 路由处理器异步化，正确 `await` 存储调用

### 🎨 UI 全面重构

- **完全重写 HTML 结构**：语义化标签、`fieldset` 分组表单、BEM 命名、更清晰的模板
- **完全重写 CSS 样式**：MD 投影体系、更优雅的状态徽章（`data-status` 驱动）、脉冲动画、展开/折叠动效
- **重构 JavaScript**：适配新 DOM 结构、增强的错误展示、状态化管理
- **响应式三端适配**：手机 / 平板 / 桌面，卡片布局自动调整
- **保持全部 6 套主题皮肤完全兼容**：`--text-secondary` 通过 `var(..., var(--muted))` 回退，无需修改皮肤文件

### 🔧 技术变更

- `package.json`: version 1.5.0 → 1.6.0
- `storage.js`: 原子写入 + 写队列 + 永不覆盖损坏文件
- `server.js`: 所有存储调用 async/await，独立运行模式自动设置 `WEISCHEDULER_DATA_DIR`
- `public/index.html`: 完全重写
- `public/styles.css`: 完全重写
- `public/app.js`: 完全重写

## 1.5.0 - 2026-06-04

### 🐛 修复

- 修复 `45 * * * *` 等 Cron 表达式无法显示为中文描述的问题 — 新增"M分每小时"匹配分支
- 全部 6 套主题对比度全面优化：辅助文字色统一提升到 WCAG AA 标准（≥4.7:1），面板背景更不透明，背景渐变不再干扰文字阅读
- Qin Empire / Witch Alchemy 两主题危险色（danger）亮化，暗色背景下清晰可辨

### 🔧 技术变更

- `package.json`: version 1.4.0 → 1.5.0
- `public/app.js`: `describeSchedule()` 新增 `M * * * *` 每小时指定分钟的中文翻译
- 所有 6 个皮肤 CSS 文件：调整 `--text`、`--muted`、`--panel`、`--danger` 色值与透明度
- `public/styles.css`: 基座变量调高对比度，`.task-card` 背景更实

## 1.4.0 - 2026-05-29

### 🌸 豆蔻少女版 UI 全面翻新

- 全新「豆蔻少女」主题：樱花粉 × 奶油白 × 淡紫渐变，玻璃拟态卡片（glassmorphism），大圆角柔光阴影
- 日系圆体字 M PLUS Rounded 1c + Nunito，精致柔软
- 花瓣粒子飘落动画 + 浮动光晕装饰 + 柔光呼吸动效

### 🎨 主题系统架构

- `styles.css` 拆分为「结构基座」+「动态皮肤 CSS」两层架构
- 新增 `preload.js` 桥接 IPC，支持主进程与渲染进程主题双向同步
- 应用菜单新增「皮肤」子菜单（radio 单选），切换即时生效
- 主题偏好持久化到 `theme.json`，重启自动恢复

### 🖌 新增 5 套主题皮肤

- 🌅 **新海诚黄昏风** — 晚霞橙 × 天空蓝 × 紫云层，城市灯火闪烁，电影光影层次
- 🌊 **深海梦境风** — 深海蓝 × 冰川青 × 荧光，水母光效，冥想沉浸氛围
- 💻 **黑客终端风** — 纯黑 × 荧光绿 × CRT 扫描线，矩阵数据流，极客赛博
- 🏯 **秦帝国黑金风** — 玄黑 × 暗金 × 青铜纹理，帝国威严布局
- 🔮 **魔女炼金风** — 深紫 × 暗金 × 月光银，魔法星象与炼金符号

### 🔧 技术变更

- `package.json`: version 1.3.2 → 1.4.0
- `electron-main.js`: 新增 `ipcMain` 主题 IPC，`setTheme()` / `buildApplicationMenu()` 重构
- 皮肤切换零闪烁：`data-theme` 属性 + 动态 `<link>` 加载
- MIT 开源协议 + 完整 README

## 1.3.2 - 2026-04-24

- 字体更换为微软雅黑，提升中文可读性
- 任务列表 Cron 表达式中文描述增强，覆盖每 N 小时/每天/每周/每月等更多模式
- 关于对话框重构为自定义窗口，布局更规范，引用语金色高亮展示
- 版本号升级至 1.3.2

## 1.3.1 - 2026-04-24

- Cron 表达式新增齿轮滑块可视化设置（简单模式/高级编辑双模式）
- 时间老人主题：暖棕/古铜金/羊皮纸色调，经典衬线字体，钟表装饰元素

## 1.3.0 - 2026-04-24

- 全面优化页面显示和布局：表单分组、任务卡片视觉升级、响应式三阶断点、状态圆点指示器、CSS 细节打磨。

## 1.2.2 - 2026-04-22

- 修复 `tasks.json` 损坏或格式非法时应用启动失败的问题；现在会自动备份损坏文件并重建空任务库继续启动。
- 补充 README 中的旧打包产物清理命令和 Windows 安装包打包说明。

## 1.2.1 - 2026-03-25

- 新增最小化到系统托盘能力，关闭窗口后调度器仍可在后台继续运行。
- 补充软件中英文简介、核心定位和版本说明，统一更新关于页元数据。
- 安装器按系统语言显示名称：英文环境为 `WeiScheduler`，中文环境为 `尉定时任务调度器`。

## 1.2.0 - 2026-03-25

- 修复跨小时 Cron 任务的稳定性问题，为 `每 3 小时`、`每 6 小时` 等表达式增加服务端补偿触发，避免整点漏执行。
- 新增 `每 6 小时` 预设，并在任务列表中显示对应的人类可读描述，方便核对任务频率。

## 1.1.10 - 2026-03-24

- 任务列表新增“下次执行”具体时间显示，直接展示下一次 Cron 触发时刻。
- 任务详情新增“下次执行”字段，方便核对任务是否按预期调度。

## 1.1.9 - 2026-03-23

- 优化 Conda 环境解析：优先读取 `conda env list --json` / `conda info --envs --json`，按环境名定位真实路径，减少对固定盘符和用户名的依赖。
- 为 `conda-name` / `conda-path` 增加执行回退策略：找不到 `python.exe` 时自动回退到 `conda run --no-capture-output`。
- 改进手动执行失败提示：点击“立即执行”或“立即执行1次”失败时，弹窗显示更具体的错误内容。
- 改进任务状态展示：失败任务在列表中显示更明确的失败原因。
- 新增 Cron 五段自由组合输入，并与原始 Cron 表达式、预设按钮双向同步。
- 修复失败日志可能被覆盖、导致只看到 `failed` 看不到具体错误的问题。

## 1.1.5

- 优化关于对话框和应用元数据。

## 1.1.1

- 发布 Windows 安装包。
